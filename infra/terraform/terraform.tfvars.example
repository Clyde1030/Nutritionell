# Copy to terraform.tfvars (gitignored) and fill in.
aws_region          = "us-east-1"
environment         = "prod"
container_image_tag = "latest"

# Provide via env instead of committing:  export TF_VAR_gemini_api_key="..."
# gemini_api_key = "your_key_here"

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.2.5

pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# 1.75.0 is the last 1.x; needed for thinking_config/thinking_budget (disabled on the
# identification + enrichment passes for a large latency win). 1.3.0 predates it.
google-genai==1.75.0

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
ultralytics>=8.1,<8.5
opencv-python-headless==4.10.0.84

# HEIC/HEIF (iPhone photo) decoding — wheels bundle libheif, no system pkg needed
pillow-heif==0.18.0

pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.28.1
aiosqlite==0.20.0
